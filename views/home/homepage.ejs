<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VMedia | The VITian Ecosystem</title>
    <link href="https://fonts.googleapis.com/css2?family=SF+Pro+Display:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #00d8ff;
            --primary-light: #008cff;
            --dark: #000;
            --dark-gray: #0a0a0a;
            --light-gray: #1a1a1a;
            --text: #fff;
            --text-secondary: #a1a1a6;
            --neon-glow: 0 0 15px rgba(0, 216, 255, 0.7);
            --gradient: linear-gradient(90deg, #00d8ff, #008cff, #00d8ff);
            --gradient-radial: radial-gradient(circle at center, #00d8ff, #008cff);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: var(--dark);
            color: var(--text);
            font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, sans-serif;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            line-height: 1.6;
        }
        
        /* Next-Level Animations */
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }
        
        @keyframes gradientFlow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
        
        @keyframes neonPulse {
            0%, 100% { text-shadow: 0 0 10px var(--primary), 0 0 20px var(--primary); }
            50% { text-shadow: 0 0 15px var(--primary), 0 0 30px var(--primary); }
        }
        
        @keyframes particleMove {
            0% { transform: translate(0, 0); opacity: 1; }
            100% { transform: translate(var(--tx), var(--ty)); opacity: 0; }
        }
        
        /* Navigation - Premium Glass Morphism */
        .nav-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            backdrop-filter: blur(12px);
            background: rgba(0, 0, 0, 0.7);
            border-bottom: 1px solid rgba(0, 216, 255, 0.1);
            transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        
        .nav-container.scrolled {
            padding: 15px 50px;
            background: rgba(0, 0, 0, 0.9);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }
        
        .logo-nav {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--text);
            text-decoration: none;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            position: relative;
         
        }
        
        .logo-nav::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--gradient);
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.6s cubic-bezier(0.19, 1, 0.22, 1);
        }
        
        .logo-nav:hover::after {
            transform: scaleX(1);
            transform-origin: left;
        }
        
        .nav-links {
            display: flex;
            gap: 40px;
        }
        
        .nav-link {
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 1rem;
            font-weight: 400;
            position: relative;
            transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .nav-link:hover {
            color: var(--text);
        }
        
        .nav-link::before {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--gradient);
            transition: width 0.6s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        
        .nav-link:hover::before {
            width: 100%;
        }
        
        .auth-buttons {
            display: flex;
            gap: 15px;
        }
        
        .auth-btn {
            padding: 10px 25px;
            border-radius: 50px;
            font-size: 0.9rem;
            text-decoration: none;
            transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
            position: relative;
            overflow: hidden;
            z-index: 1;
        }
        
        .auth-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient);
            z-index: -1;
            opacity: 0;
            transition: opacity 0.5s ease;
        }
        
        .login-btn {
            color: var(--text);
            background: transparent;
            border: 1px solid rgba(0, 216, 255, 0.3);
        }
        
        .login-btn:hover {
            color: var(--text);
            border-color: var(--primary);
            box-shadow: var(--neon-glow);
        }
        
        .login-btn:hover::before {
            opacity: 0.2;
        }
        
        .signup-btn {
            color: var(--dark);
            background: var(--gradient);
            font-weight: 500;
            background-size: 200% auto;
            animation: gradientFlow 3s infinite linear;
        }
        
        .signup-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 140, 255, 0.3);
        }
        
        /* Hero Section - Cosmic Effect */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 2rem;
            position: relative;
            overflow: hidden;
            background: radial-gradient(ellipse at bottom, #0d0d1a 0%, #000000 100%);
        }
        
        .particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }
        
        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: var(--primary);
            border-radius: 50%;
            animation: particleMove linear infinite;
        }
        
        .hero-video {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.15;
            z-index: 0;
            mix-blend-mode: screen;
        }
        
        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 1000px;
            margin: 0 auto;
        }
        
        .logo {
            font-size: 6rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: -2px;
            text-align: center;
        }
        
        .tagline {
            font-size: 2.5rem;
            font-weight: 400;
            margin-bottom: 2rem;
            color: var(--text);
            line-height: 1.2;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.1);
            text-align: center;
        }
        
        .subtagline {
            font-size: 1.3rem;
            color: var(--text-secondary);
            margin-bottom: 3rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            opacity: 0.9;
            text-align: center;
        }
        
        .cta-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 2rem;
        }
        
        .cta {
            padding: 15px 30px;
            border-radius: 50px;
            font-size: 1.1rem;
            text-decoration: none;
            transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
            position: relative;
            overflow: hidden;
            z-index: 1;
            font-weight: 500;
            letter-spacing: 1px;
        }
        
        .cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient);
            z-index: -1;
            transition: all 0.5s ease;
        }
        
        .cta-primary {
            color: var(--text);
            border: 1px solid transparent;
        }
        
        .cta-primary::before {
            opacity: 1;
            background-size: 200% auto;
            animation: gradientFlow 3s infinite linear;
        }
        
        .cta-primary:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 140, 255, 0.4);
        }
        
        .cta-secondary {
            color: var(--primary);
            border: 1px solid var(--primary);
            background: transparent;
        }
        
        .cta-secondary::before {
            opacity: 0;
            background: var(--gradient);
        }
        
        .cta-secondary:hover {
            color: var(--text);
            border-color: transparent;
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 140, 255, 0.3);
        }
        
        .cta-secondary:hover::before {
            opacity: 1;
        }
        
        .scroll-down {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            color: var(--text-secondary);
            font-size: 1.5rem;
            cursor: pointer;
            animation: float 2s ease-in-out infinite;
            transition: all 0.3s ease;
        }
        
        .scroll-down:hover {
            color: var(--primary);
            animation: float 1.5s ease-in-out infinite;
        }
        
        /* Features Section - Holographic Cards */
        .features {
            padding: 10rem 0;
            position: relative;
            background: var(--dark);
            overflow: hidden;
        }
        
        .features::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, rgba(0, 216, 255, 0.05) 0%, transparent 70%);
            pointer-events: none;
        }
        
        .section-title {
            font-size: 4rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 6rem;
            padding: 0 2rem;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            position: relative;
            display: inline-block;
            width: 100%;
        }
        
        .section-title::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: var(--gradient);
            border-radius: 3px;
        }
        
        /* Horizontal Scroll Container */
        .horizontal-scroll-container {
            display: flex;
            overflow-x: auto;
            gap: 2rem;
            padding: 2rem;
            scrollbar-width: none; /* Firefox */
            -ms-overflow-style: none; /* IE and Edge */
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .horizontal-scroll-container::-webkit-scrollbar {
            display: none; /* Chrome, Safari, Opera */
        }
        
        .feature-card {
            flex: 0 0 300px;
            background: rgba(10, 10, 10, 0.7);
            border-radius: 20px;
            padding: 2.5rem;
            transition: all 0.6s cubic-bezier(0.165, 0.84, 0.44, 1);
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 216, 255, 0.1);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }
        
        .feature-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: var(--gradient-radial);
            opacity: 0.1;
            transform: rotate(45deg);
            transition: all 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
            z-index: -1;
        }
        
        .feature-card:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0, 140, 255, 0.2);
            border-color: rgba(0, 216, 255, 0.3);
        }
        
        .feature-card:hover::before {
            transform: rotate(90deg);
            opacity: 0.2;
        }
        
        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            color: var(--primary);
            text-shadow: 0 0 10px rgba(0, 216, 255, 0.3);
            text-align: center;
        }
        
        .feature-card h3 {
            font-size: 1.7rem;
            margin-bottom: 1rem;
            font-weight: 600;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-align: center;
        }
        
        .feature-card p {
            color: var(--text-secondary);
            line-height: 1.6;
            font-size: 1.1rem;
            text-align: center;
        }
        
        /* Stats Section - Animated Counters */
        .stats {
            padding: 10rem 2rem;
            background: linear-gradient(to bottom, var(--dark), var(--dark-gray));
            position: relative;
            overflow: hidden;
        }
        
        .stats::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" preserveAspectRatio="none"><path d="M0,0 L100,0 L100,100 Q50,80 0,100 Z" fill="rgba(0,216,255,0.03)"/></svg>');
            background-size: 100% auto;
            background-repeat: no-repeat;
            background-position: bottom;
            opacity: 0.5;
            pointer-events: none;
        }
        
        .stats-grid {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            max-width: 1200px;
            margin: 0 auto;
            gap: 4rem;
        }
        
        .stat-item {
            text-align: center;
            min-width: 200px;
            position: relative;
        }
        
        .stat-item::after {
            content: '';
            position: absolute;
            bottom: -20px;
            left: 50%;
            transform: translateX(-50%);
            width: 50px;
            height: 3px;
            background: var(--gradient);
            border-radius: 3px;
            opacity: 0.7;
        }
        
        .stat-number {
            font-size: 4.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-family: 'SF Pro Display', sans-serif;
        }
        
        .stat-label {
            font-size: 1.2rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        /* Testimonials - 3D Card Effect */
        .testimonials {
            padding: 10rem 2rem;
            background: var(--dark);
            position: relative;
            perspective: 1000px;
        }
        
        .testimonials::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, rgba(0, 216, 255, 0.05) 0%, transparent 70%);
            pointer-events: none;
        }
        
        .testimonial-card {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(10, 10, 10, 0.7);
            border-radius: 20px;
            padding: 4rem;
            text-align: center;
            position: relative;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 216, 255, 0.1);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            transform-style: preserve-3d;
            transition: all 0.5s ease;
        }
        
        .testimonial-card:hover {
            transform: rotateY(5deg) rotateX(5deg) translateY(-10px);
            box-shadow: 0 30px 60px rgba(0, 140, 255, 0.2);
        }
        
        .testimonial-text {
            font-size: 1.6rem;
            font-weight: 300;
            margin-bottom: 3rem;
            line-height: 1.6;
            position: relative;
            font-style: italic;
        }
        
        .testimonial-text::before,
        .testimonial-text::after {
            content: '"';
            font-size: 4rem;
            color: var(--primary);
            opacity: 0.3;
            position: absolute;
            font-family: serif;
        }
        
        .testimonial-text::before {
            top: -2rem;
            left: -1.5rem;
        }
        
        .testimonial-text::after {
            bottom: -3.5rem;
            right: -1.5rem;
        }
        
        .testimonial-author {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1.5rem;
        }
        
        .author-avatar {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--primary);
            box-shadow: 0 0 20px rgba(0, 216, 255, 0.3);
        }
        
        .author-info h4 {
            font-size: 1.4rem;
            margin-bottom: 0.3rem;
            text-align: left;
            color: var(--text);
        }
        
        .author-info p {
            color: var(--text-secondary);
            text-align: left;
            font-size: 1rem;
        }
        
        /* Footer - Futuristic Design */
        footer {
            padding: 8rem 2rem 3rem;
            background: linear-gradient(to bottom, var(--dark-gray), #000);
            position: relative;
            overflow: hidden;
        }
        
        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 1px;
            background: var(--gradient);
            box-shadow: 0 0 20px rgba(0, 216, 255, 0.5);
        }
        
        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 4rem;
        }
        
        .footer-logo {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-align: center;
        }
        
        .footer-about p {
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
            line-height: 1.6;
            font-size: 1.1rem;
            text-align: center;
        }
        
        .social-links {
            display: flex;
            gap: 1rem;
            justify-content: center;
        }
        
        .social-link {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(255, 255, 255, 0.05);
            color: var(--text);
            transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
            text-decoration: none;
            position: relative;
            overflow: hidden;
        }
        
        .social-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient);
            z-index: -1;
            opacity: 0;
            transition: opacity 0.5s ease;
        }
        
        .social-link:hover {
            transform: translateY(-5px);
            color: var(--text);
        }
        
        .social-link:hover::before {
            opacity: 1;
        }
        
        .footer-links h3 {
            font-size: 1.4rem;
            margin-bottom: 1.5rem;
            font-weight: 600;
            color: var(--text);
            position: relative;
            display: inline-block;
            text-align: center;
            width: 100%;
        }
        
        .footer-links h3::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            width: 50%;
            height: 2px;
            background: var(--gradient);
        }
        
        .footer-links ul {
            list-style: none;
            text-align: center;
        }
        
        .footer-links li {
            margin-bottom: 1rem;
        }
        
        .footer-links a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
            position: relative;
        }
        
        .footer-links a::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 0;
            height: 1px;
            background: var(--gradient);
            transition: width 0.3s ease;
        }
        
        .footer-links a:hover {
            color: var(--primary);
            transform: translateX(5px);
        }
        
        .footer-links a:hover::after {
            width: 100%;
        }
        
        .footer-newsletter {
            text-align: center;
        }
        
        .footer-newsletter input {
            width: 100%;
            padding: 15px;
            border-radius: 5px;
            border: none;
            background: rgba(255, 255, 255, 0.1);
            color: var(--text);
            margin-bottom: 1rem;
            font-size: 1rem;
            transition: all 0.3s ease;
        }
        
        .footer-newsletter input:focus {
            outline: none;
            background: rgba(255, 255, 255, 0.15);
            box-shadow: 0 0 10px rgba(0, 216, 255, 0.2);
        }
        
        .footer-newsletter button {
            width: 100%;
            padding: 15px;
            border-radius: 5px;
            border: none;
            background: var(--gradient);
            color: var(--text);
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1rem;
            background-size: 200% auto;
            animation: gradientFlow 3s infinite linear;
        }
        
        .footer-newsletter button:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 140, 255, 0.3);
        }
        
        .copyright {
            text-align: center;
            margin-top: 6rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: var(--text-secondary);
            font-size: 0.9rem;
            position: relative;
        }
        
        .copyright::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 2px;
            background: var(--gradient);
        }
        
        /* Responsive Design */
        @media (max-width: 992px) {
            .nav-links {
                display: none;
            }
            
            .logo {
                font-size: 4.5rem;
            }
            
            .tagline {
                font-size: 2rem;
            }
            
            .subtagline {
                font-size: 1.1rem;
            }
            
            .section-title {
                font-size: 3rem;
            }
            
            .stat-number {
                font-size: 3.5rem;
            }
            
            .testimonial-text {
                font-size: 1.4rem;
            }
        }
        
        @media (max-width: 768px) {
            .nav-container {
                padding: 15px 20px;
            }
            
            .logo {
                font-size: 3.5rem;
            }
            
            .tagline {
                font-size: 1.8rem;
            }
            
            .cta-container {
                flex-direction: column;
                gap: 15px;
            }
            
            .section-title {
                font-size: 2.5rem;
            }
            
            .stat-item {
                min-width: 150px;
            }
            
            .stat-number {
                font-size: 2.5rem;
            }
            
            .feature-card {
                padding: 2rem;
            }
            
            .testimonial-card {
                padding: 2rem;
            }
            
            .testimonial-text {
                font-size: 1.2rem;
            }
            
            .footer-content {
                grid-template-columns: 1fr;
                gap: 3rem;
            }
        }
        
   
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: var(--dark);
        }
        
        ::-webkit-scrollbar-thumb {
            background: var(--gradient);
            border-radius: 10px;
        }
        
      
        [data-animate] {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        
        [data-animate].animate {
            opacity: 1;
            transform: translateY(0);
        }
        
     
        .delay-1 { transition-delay: 0.1s; }
        .delay-2 { transition-delay: 0.2s; }
        .delay-3 { transition-delay: 0.3s; }
        .delay-4 { transition-delay: 0.4s; }
        .delay-5 { transition-delay: 0.5s; }
        .intro-bg-video {
  position: absolute;
  top: 50%;
  left: 50%;
  min-width: 100%;
  min-height: 100%;
  width: auto;
  height: auto;
  transform: translate(-50%, -50%);
  object-fit: cover;
  z-index: -1; /* behind the loader block */
  pointer-events: none; /* so clicks go through video */
}
.video-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.4); /* 🟡 Adjust opacity here */
  z-index: 0;
}
 /* --- INTRO SECTION --- */
    #intro {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      transition: opacity 1s ease;
      z-index: 2;
    }



    .loader-container {
      font-size: 4rem;
      font-weight: bold;
      display: flex;
      align-items: center;
    }

 .v-symbol {
  opacity: 0;
  animation: fadeIn 1s ease forwards, softGlow 3s ease-in-out infinite alternate;
  animation-delay: 0.5s;
  font-weight: bold;
  background: linear-gradient(90deg, #00d8ff, #008cff);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  text-shadow:
    0 0 6px rgba(0, 136, 255, 1),
    0 0 12px rgba(0, 136, 255, 1);
}


    </style>
</head>
<body>

<div id="main">
    <nav class="nav-container">
        <a href="#" class="logo-nav">VALL</a>
        &nbsp;
        
        <div class="nav-links">
            <a href="#features" class="nav-link">Features</a>
            <a href="#stats" class="nav-link">Stats</a>
            <a href="#testimonials" class="nav-link">Testimonials</a>
            <a href="/aboutus" class="nav-link">About</a>
        </div>
        
       <div class="auth-buttons">
  <% if (user) { %>
    <a href="/" class="auth-btn login-btn">Home</a>
  <% } else { %>
    <a href="/users/login" class="auth-btn login-btn">Login</a>
    <a href="/users/signup" class="auth-btn signup-btn">Sign Up</a>
  <% } %>
</div>
    </nav>
    
 
    <section class="hero" id="home">
       
       
        <div class="hero-content animate-fade" style="animation-delay: 0.3s;">
            <h1 class="logo">VALL</h1>
            <h2 class="tagline">The VITian Ecosystem</h2>
            <p class="subtagline">Connect, collaborate, and conquer your academic journey with our all-in-one platform designed exclusively for VIT students.</p>
            
            <div class="cta-container">
                <a href="/users/signup" class="cta cta-primary">Get Started</a>
                <a href="#features" class="cta cta-secondary">Learn More</a>
            </div>
        </div>
        
        <a href="#features" class="scroll-down">
            <i class="fas fa-chevron-down"></i>
        </a>
    </section>
    

    <section class="features" id="features">
        <h2 class="section-title animate-fade" style="animation-delay: 0.2s;">Everything you need. All in one place.</h2>
        
        <div class="horizontal-scroll-container">
            <div class="feature-card animate-fade" style="animation-delay: 0.3s;">
                <div class="feature-icon">
                    <i class="fas fa-calculator"></i>
                </div>
                <h3>CGPA Calculator</h3>
                <p>Track your academic performance with precision and plan your semester goals with our advanced CGPA calculator.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 0.4s;">
                <div class="feature-icon">
                    <i class="fas fa-calendar-alt"></i>
                </div>
                <h3>Smart Study Planner</h3>
                <p>AI-powered scheduling that adapts to your learning patterns and exam calendar for optimal study sessions.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 0.5s;">
                <div class="feature-icon">
                    <i class="fas fa-layer-group"></i>
                </div>
                <h3>Flashcards & Quizlets</h3>
                <p>Collaborative flashcard system with spaced repetition algorithms to enhance your learning efficiency.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 0.6s;">
                <div class="feature-icon">
                    <i class="fas fa-home"></i>
                </div>
                <h3>Roommate Finder</h3>
                <p>Algorithmic matching based on habits, schedules, and compatibility to find your perfect roommate.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 0.7s;">
                <div class="feature-icon">
                    <i class="fas fa-comment-dots"></i>
                </div>
                <h3>Confessions Wall</h3>
                <p>Anonymous sharing platform with sentiment analysis and community support features.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 0.8s;">
                <div class="feature-icon">
                    <i class="fas fa-search"></i>
                </div>
                <h3>Lost & Found Board</h3>
                <p>Campus-wide digital board to report lost items or claim found belongings with image recognition.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 0.9s;">
                <div class="feature-icon">
                    <i class="fas fa-calendar-check"></i>
                </div>
                <h3>FFCS Planner</h3>
                <p>Visual course selection tool with conflict detection and professor ratings for optimal FFCS planning.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 1.0s;">
                <div class="feature-icon">
                    <i class="fas fa-chalkboard-teacher"></i>
                </div>
                <h3>Faculty Review</h3>
                <p>Anonymous professor ratings and reviews to help you make informed course selection decisions.</p>
            </div>
            
            <div class="feature-card animate-fade" style="animation-delay: 1.1s;">
                <div class="feature-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h3>Collab Requests</h3>
                <p>Find project partners, study groups, or research collaborators across different disciplines.</p>
            </div>
            
           
            <div class="feature-card animate-fade" style="animation-delay: 0.8s;">
                <div class="feature-icon">
                    <i class="fas fa-comment-dots"></i>
                </div>
                <h3>Previous Year Papers</h3>
                <p>Anonymous sharing with sentiment analysis and community support.</p>
            </div>
        </div>
    </section>
    
   
     <section class="stats" id="stats">
      <div class="stats-grid">
       <div class="stat-item animate-fade" style="animation-delay: 0.3s;">
  <div class="stat-number" id="user-count">5,000+</div>
  <div class="stat-label">Active Users</div>
</div>
        <div class="stat-item animate-fade" style="animation-delay: 0.4s;">
          <div class="stat-number">95%</div>
          <div class="stat-label">Satisfaction Rate</div>
        </div>
        <div class="stat-item animate-fade" style="animation-delay: 0.5s;">
  <div class="stat-number" id="view-count">1M+</div>
  <div class="stat-label">Total Views</div>
</div>
        <div class="stat-item animate-fade" style="animation-delay: 0.6s;">
          <div class="stat-number">24/7</div>
          <div class="stat-label">Support</div>
        </div>
      </div>
    </section>

    

    <section class="testimonials" id="testimonials">
        <h2 class="section-title animate-fade" style="animation-delay: 0.2s;">What VITians Say</h2>
        
        <div class="testimonial-card animate-fade" style="animation-delay: 0.4s;">
            <p class="testimonial-text">The Project Collab feature on VALL made finding like-minded teammates so easy! I could post my project idea, filter applicants, and build a team that actually matched my goals. It turned collaboration from chaos into clarity!</p>
            <div class="testimonial-author">
                <img src="https://randomuser.me/api/portraits/men/32.jpg" alt="Student" class="author-avatar">
                <div class="author-info">
                    <h4>Rahul Sharma</h4>
                    <p>Computer Science, 3rd Year</p>
                </div>
            </div>
        </div>
    </section>
    

    <footer>
      <div class="footer-content">
        <div class="footer-about">
          <div class="footer-logo">VALL</div>
          <p>The ultimate digital ecosystem designed exclusively for VIT students.</p>
          <div class="social-links">
          
            <a href="#" class="social-link"><i class="fab fa-instagram"></i></a>
            <a href="#" class="social-link"><i class="fab fa-linkedin-in"></i></a>
          </div>
        </div>

        <div class="footer-links">
          <h3>Quick Links</h3>
          <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#features">Features</a></li>
            <li><a href="#testimonials">Testimonials</a></li>
            <li><a href="#">About Us</a></li>
            <li><a href="#">Contact</a></li>
          </ul>
        </div>

        <div class="footer-links">
          <h3>Resources</h3>
          <ul>
            <li><a href="/tasks">Study Materials</a></li>
            <li><a href="/api/pyq">Previous Year Papers</a></li>
            <li><a href="/faculty">Faculty Reviews</a></li>
            <li><a href="/cgpa">Campus Map</a></li>
            <li><a href="/lost-and-found">Event Calendar</a></li>
          </ul>
        </div>

      </div>

      <div class="copyright">
        <p>&copy; 2025 VMedia. All rights reserved. Designed for VITians, by VITians.</p>
      </div>
    </footer>
</div>
    <script>
        
        const animateElements = document.querySelectorAll('.animate-fade');

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }
      });
    }, { threshold: 0.1 });

    animateElements.forEach(el => {
      el.style.opacity = '0';
      el.style.transform = 'translateY(20px)';
      el.style.transition = 'all 0.6s ease';
      observer.observe(el);
    });
    //end 

    const fullWords = ["Victory", "VITian", "Venture", "Vision", "Virtue", "Velocity", "Versatile"];
    const suffixEl = document.getElementById("suffix");
    const tagline = document.getElementById("tagline");
    const tickSound = document.getElementById("tick-sound");
    const intro = document.getElementById("intro");
    const main = document.getElementById("main");

    let index = 1;
    let count = 0;
    const max = fullWords.length;
    let intervalId;

    function updateSuffix() {
      suffixEl.classList.remove("fade-in");
      suffixEl.classList.add("fade-out");

      setTimeout(() => {
        if (count >= max) {
          suffixEl.textContent = "ALL";
          suffixEl.classList.add("glow");
          clearInterval(intervalId);

          tickSound.play();
          tagline.textContent = "VIT for ALL";
          tagline.classList.add("show");

          // Wait and then show main site
          setTimeout(() => {
            intro.style.opacity = 0;
            setTimeout(() => {
              intro.style.display = "none";
              main.style.display = "block";
            }, 1000); // matches transition
          }, 2000); // after tagline
        } else {
          const word = fullWords[index % fullWords.length];
          suffixEl.textContent = word.substring(1);
          index++;
          count++;
        }

        suffixEl.classList.remove("fade-out");
        suffixEl.classList.add("fade-in");
      }, 100);
    }

    setTimeout(() => {
      document.getElementById("v-symbol").style.opacity = 1;
      suffixEl.classList.add("fade-in");
      intervalId = setInterval(updateSuffix, 500);
    }, 500);

    window.addEventListener('DOMContentLoaded', () => {
    fetch('/home/api/users/count')
      .then(response => response.json())
      .then(data => {
        document.getElementById('user-count').textContent = 
          data.count.toLocaleString();
      })
      .catch(err => {
        console.error('Error fetching user count:', err);
        // Keep the default "5,000+" if there's an error
      });
  });
  // Update the user count if it was passed from the server
 window.addEventListener('DOMContentLoaded', () => {
    // Track the view
    fetch('/home/api/views/increment')
      .then(response => response.json())
      .then(data => {
        // Update the view count display
        if (data.total) {
          document.getElementById('view-count').textContent = 
            data.total >= 1000000 ? 
            `${(data.total/1000000).toFixed(1)}M+` : 
            data.total.toLocaleString();
        }
      })
      .catch(err => {
        console.error('Error tracking view:', err);
      });

    // Also fetch the total views separately (in case increment fails)
    fetch('/home/api/views/total')
      .then(response => response.json())
      .then(data => {
        if (data.total) {
          document.getElementById('view-count').textContent = 
            data.total >= 1000000 ? 
            `${(data.total/1000000).toFixed(1)}M+` : 
            data.total.toLocaleString();
        }
      })
      .catch(err => {
        console.error('Error getting total views:', err);
      });
  });
  // A
    </script>
</body>
</html>
